namespace Tic_Tac_Toe_Tests
{
    public class JudgeTests
    {
        //DetermineWinner() should return either a Player or null.
        //When it is a draw, DetermineWinner() should return null.
        //When 3 cells on the same row has same value -> a winner (player) is returned.
        //When 3 cells on the same column has same value -> a winner (player) is returned.
        //When 3 cells on the same diagonal has same value -> a winner (player) is returned.
        //When Player 1 (X) wins, DetermineWinner should return Player 1.
        //When Player 2 (O) wins, DetermineWinner should return Player 2. -> return correct winner.
    }
}